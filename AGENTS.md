# MBD H2 KR dashboard 작업 규칙

이 파일은 이 저장소에만 적용한다. 다른 workspace의 지침이나 데이터를 수정하지 않는다.

## 변경 전 필수 확인

1. `data/source_contract.json`, `docs/source-of-truth.md`, `docs/dashboard-refresh-contract.md`를 먼저 읽는다.
2. 요청한 화면 항목을 원천·필드·기간·모집단·단위·조회시각에 연결한다. 실제 DB/시트의 스키마와 데이터로 확인하며 문서 존재만으로 연결 완료를 주장하지 않는다.
3. 현재 사용자 승인 범위, 변경할 파일, 확인할 결과를 정한다. 기존 승인은 유지되며 이 규칙은 이미 승인된 구현에 재승인을 요구하지 않는다.
4. registry와 실제 원천이 다르면 차이를 드러내고 승인된 범위 안에서 함께 바로잡는다. 원천을 확인하지 않고 가까운 이름의 시트·열·테이블로 대체하지 않는다.

## 전체 원천과 화면의 정합성

- 전체 편성 수와 발행/실적 수를 별도로 검증한다. 원천 행 → 유효 행 → 날짜만 있는 빈 구좌 → 미정/예정/발행/미매칭 → 화면 행·요약 수의 관계를 설명할 수 있어야 한다. 표제·메타데이터 행의 제외 사유도 확인한다.
- 일정에 ID가 없어도 버리거나 가짜 ID를 만들지 않는다. YouTube 발행 실적은 검증된 video ID로만 연결하며 제목 유사성으로 매칭하지 않는다. 충돌·중복·월 경계·재편성·삭제를 다룬다.
- `확인 못 함`은 증거 부족/조회 실패, `미연결`은 소비 경로 부재, `검증된 없음`은 정상 조회·스키마·범위 검증 후 0건이다. 세 상태를 0, 미편성, 연결 완료로 뭉개지 않는다.
- 미래·미발행·미매칭 지표는 `—`다. 예정 편성이 발행 수, D7 평균, 수익 또는 품질 모집단을 바꾸면 안 된다.
- YouTube 일정에는 독립 취소 열이 확인되지 않았다. 제목·색상·빈 ID로 취소를 추정하지 않는다.
- source cutoff, source collection time, metrics snapshot time, build time을 구분한다. 과거 실적의 지연 갱신이 매출 확정을 다시 열어서는 안 된다.

## 변경·공개 경계

- 사용자 요청으로 승인된 로컬 캐시·코드 변경만 수행한다. 원본 시트, canonical DB, 크론, 인증, provider, gateway를 임의로 변경하지 않는다. 이를 변경하려면 그 대상에 대한 명시적 승인이 필요하다.
- 이 요청에서 승인된 YouTube 수기 공개 범위는 A:G의 날짜·시간·포맷·IP·콘텐츠명과 검증된 YouTube 링크다. H:O 성과를 예정행 지표로 사용하지 않는다.
- 신원정보, 담당자/PD, 비용·마진, 내부 메모, 비밀·인증정보는 HTML, 툴팁, data 속성, JSON manifest, 로그에 내보내지 않는다. 원천 값을 HTML escape하고 링크를 allowlist로 검증한다.
- snapshot은 원천을 읽기 전용으로 수집하여 별도 후보 캐시에 검증한 뒤 교체한다. API 실패를 빈 일정으로 바꾸거나 실패 중 마지막 정상 산출물을 덮어쓰지 않는다.
- commit/push/배포/외부 메시지는 해당 작업에 대한 사용자 승인이 있을 때만 수행한다. 승인된 작업은 불필요한 재확인 없이 계속 진행한다.

## 완료 기준

- 관련 회귀 테스트, `git diff --check`, static/contract 검증, 필요한 브라우저 검증을 통과시킨다. 동시 실행 중인 ops/full-suite fixture 작업과 충돌하지 않도록 조정한다.
- 전체 원천의 포함·제외·중복·빈 구좌 수와 화면 수를 대조하고, 편성 추가 전후 발행/품질/매출 분모가 유지되는지 확인한다.
- 배포가 승인된 작업은 정확한 배포 revision의 공개 HTML/계약을 다시 읽고 검증해야 완료다. 배포가 범위 밖이면 로컬 검증 수준까지만 보고한다.
- 테스트나 필수 probe가 실패하거나 실행 불가하면 `확인 못 함`과 원인을 적는다. 수집됨·캐시됨·렌더됨·공개됨을 구분하여 간결하게 한국어로 보고한다.
